server:
  port: 50004
spring:
  application:
    name: game-fun-weMedia
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      config:
        file-extension: yaml
        extension-configs[0]: datasource.yaml
        extension-configs[1]: redis.yaml
        extension-configs[2]: baiduAi.yaml
        extension-configs[3]: security.yaml
